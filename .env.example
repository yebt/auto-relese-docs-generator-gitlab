# GitLab Configuration
# Get your access token from: https://gitlab.com/-/profile/personal_access_tokens
# Required scopes: api, read_api, read_repository
GITLAB_ACCESS_TOKEN=your_gitlab_access_token_here

# GitLab Project ID
# You can find this in your project's Settings > General
GITLAB_PROJECT_ID=your_project_id_here

# Gemini AI Token (OPCIONAL - solo necesario si usas --api)
# Por defecto, el generador usa Gemini CLI (local) que no requiere API key
# Si prefieres usar la API de Gemini, obtén tu API key desde:
# https://aistudio.google.com/app/apikey
GEMINI_TOKEN=your_gemini_api_key_here
